1) Understanding of jvm  --------------> https://youtu.be/6ZquiVH8AGU?feature=shared
2) indexing in db -------------------->https://youtu.be/6ZquiVH8AGU?feature=shared ------ both same youtube channel

3) Project 1 (End of Month 1) ✅ Build a URL Shortener (Java + Spring Boot + Redis) ----- instead of reddis used postgresz , pushed code here 
https://github.com/vijayjava15/projectspring/tree/master/urlShortner/urlShortner  all spring projects would be here , check for latest update.....

4) spring ioc , @Service, @Component, @Repository, @Controller annotation meaning

5)Selection sort, Factory design pattern (PENDING)   abstract vs inheritance (PENDING)

6)  started learning websocket done a little project (more updates come)

//leaving space for webSocket//







7) started learning java socket programming and tcp concepts )))) ----------------> PENDING

8) * Deep dive into **Entity Lifecycle** — State transitions, `persist()`, `merge()`, `detach()`, `remove()`.
  - **Problem:** Build a state tracker that logs every entity state change automatically.
:** Understand **Persistence Context** & Flushing Strategies.
